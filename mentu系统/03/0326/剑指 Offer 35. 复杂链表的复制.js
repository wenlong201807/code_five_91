/**
 * // Definition for a Node.
 * function Node(val, next, random) {
 *    this.val = val;
 *    this.next = next;
 *    this.random = random;
 * };
 */

/**
 * @param {Node} head
 * @return {Node}
 * 1 创建两个指针，一个指向头指针 ，另一个指向第一个克隆节点
 * 拆分链表：原节点和克隆节点
 */
var copyRandomList = function (head) {

};