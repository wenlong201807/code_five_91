/**
 * Definition for singly-linked list.
 * function ListNode(val) {
 *     this.val = val;
 *     this.next = null;
 * }
 */

/**
 * @param {ListNode} head
 * @return {ListNode}
 * 思路
 * 快慢指针
 * 快指针走两步，慢指针走一步，一开始，我们的快慢指针指向同一个位置，当我们的快指针遇上了慢指针，说明链表是有一个环的。
 * 
 */
var detectCycle = function (head) {

};